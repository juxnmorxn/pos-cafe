# Configuración para Render.com
services:
  - type: web
    name: menu-cafeteria
    env: node
    buildCommand: npm install && npm run build
    startCommand: node server.js
    envVars:
      - key: PORT
        value: 3000
      - key: NODE_ENV
        value: production
